// Child model that extends parent with REQUIRED repo_url
// This represents the "before" state

extends "./public_required.cdm"

@sql { dialect: "postgresql" }

// Make parent model skip SQL table generation
PublicDaemonAuthRequest {
  @sql { skip: true }
} #10

// Child model inherits repo_url from parent
DaemonAuthRequest extends PublicDaemonAuthRequest {
  daemon_id?: string #1
  @sql {
    indexes: {
      request_id: { fields: ["request_id"], unique: true }
    }
  }
} #14
